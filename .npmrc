legacy-peer-deps=true
# Puppeteer skips the Chromium download via the PUPPETEER_SKIP_DOWNLOAD env
# var set in vercel.json. Locally, run
#   PUPPETEER_SKIP_DOWNLOAD=false npx puppeteer browsers install chrome
# the one time you want the capture-*.mjs scripts to work.
